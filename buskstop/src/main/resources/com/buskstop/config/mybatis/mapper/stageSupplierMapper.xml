<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.buskstop.config.mybatis.mapper.stageSupplierMapper">
	
	<insert id="insertStageSupplier" parameterType="PremiumStage">
		INSERT INTO STAGE_SUPPLIER
		(ESTABLISH_NO, OPERATOR_USER_ID, OPERATOR_NO, STAGE_NAME, STAGE_LOCATION, STAGE_AREA, STAGE_IMAGE)
		VALUES(
			#{establishNum},#{userId},#{operatorNum},#{stageName},#{stageLocation},#{stageArea},#{stageImage}
		)
	</insert>
	
	<update id="updateStageSupplier" parameterType="PremiumStage">
		UPDATE	STAGE_SUPPLIER
		SET		OPERATOR_USER_ID = #{userId},
				OPERATOR_NO = #{operatorNum},
				STAGE_NAME = #{stageName},
				STAGE_LOCATION = #{stageLocation},
				STAGE_AREA = #{stageArea},
				STAGE_IMAGE = #{stageImage}
		WHERE 	ESTABLISH_NO = #{establishNum}				
	</update>
	
	<select id="selectSupplierById" parameterType="string" resultType="PremiumStage">
		SELECT	*
		FROM	STAGE_SUPPLIER
		WHERE	OPERATOR_USER_ID = #{userId}
	</select>
</mapper>